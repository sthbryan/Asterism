# Asterism

Desktop app for tracking GitHub stars, forks, and release downloads for a selected set of repositories.

Requires [GitHub CLI](https://cli.github.com) (`gh`) signed in.

```bash
bun install
bun run tauri dev
```

Selection is stored at `~/.config/asterism/config.json`.
